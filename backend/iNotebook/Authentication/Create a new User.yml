info:
  name: Create a new User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/createuser
  body:
    type: json
    data: |-
      {
        "name": "Ravi Gupta12",
        "email": "ravi12@gmail.com",
        "password": "ravi@email1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
